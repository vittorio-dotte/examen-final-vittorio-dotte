version: '3.8'

services:
  web_server:
    image: nginx:latest
    container_name: examen_web
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html
    restart: always
